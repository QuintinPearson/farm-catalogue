<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= farm.name %> Details</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
  </head>
  <body>
    <%= message %>
    <div class="container">
      <h1><%= farm.name %></h1>
      <div class="row">
        <div class="col-md-6">
          <p><strong>Location:</strong> <%= farm.location %></p>
          <p><strong>Email:</strong> <%= farm.email %></p>
          <% if (farm.products && farm.products.length > 0) { %>
          <h2>Products:</h2>
          <ul>
            <% farm.products.forEach(product => { %>
            <a href="/products/<%= product._id %>"
              ><li><%= product.name %></li></a
            >
            <% }) %>
          </ul>
          <% } else { %>
          <p>No products listed for this farm.</p>
          <% } %>
        </div>
      </div>
      <div>
        <a href="/farms/<%= farm.id %>/products/new" class="btn btn-info mt-3"
          >Create New Product</a
        >
        <a href="/farms" class="btn btn-secondary mt-3">Back to All Farms</a>
      </div>
      <div>
        <form
          class="mt-3"
          action="/farms/<%= farm.id %>?_method=DELETE"
          method="POST"
        >
          <button class="btn btn-danger">Delete Farm</button>
        </form>
      </div>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeATEFsHoHLcW0oPxkp2iDJVuzaMozEwYise/w2ZApKbDVPCu"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
